{
  "mcpServers": {
    "prompt": {
      "command": "python",
      "args": ["server.py"],
      "env": {
        "PYTHONPATH": "../../",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}
